# Public app metadata exposed to the client.
NUXT_PUBLIC_APP_NAME=revo-grid-poc
NUXT_PUBLIC_APP_URL=http://localhost:3000

# Default locale used by the app when nothing else is selected.
NUXT_PUBLIC_DEFAULT_LOCALE=fr
